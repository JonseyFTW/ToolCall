Flask>=2.0,<3.0
qwen-agent[code_interpreter]==0.0.24  # Pinning to a specific version WITH code_interpreter extras
playwright==1.39.0  # Pinning to version compatible with qwen-agent==0.2.2
python-dateutil>=2.8.0
python-dotenv>=0.19.0
# beautifulsoup4 and lxml might be indirect dependencies or useful for custom parsing if extending
# matplotlib will be installed as part of qwen-agent[code_interpreter]